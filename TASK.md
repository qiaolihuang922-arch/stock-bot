# TASK: v19.5 收盤決策壓縮與執行清單升級

## 時間性

- 任務日期：2026-05-26
- 來源任務：`RESEARCH.md` Architect Conclusion
- 目標版本：`v19.5`
- 任務性質：報文結構 / 決策摘要 / 顯示排序升級
- version_level：`minor`
- qa_level：`L3` 預設；若 Architect / Owner 明確限定不改策略、DB、replay/backfill，實測可收斂為 `L2 顯示不變性 + Telegram contract`，但不得低於 L2。
- 狀態：PM 正式需求，等待 Architect 交 Tech
- 邊界：只定義顯示層、排序層、summary view model 需求；不改策略門檻。

## 需求目標

v19.5 要把 v19.4.1 的收盤報文升級為「30 秒可決策」的交易執行摘要。

使用者打開 Telegram 最下面的摘要時，必須能快速知道：

- 今天是否新增。
- 明天最重要的 3-5 件事。
- 哪些持倉要優先風控或觀察。
- 哪些未持倉值得追蹤。
- 哪些標的只是淘汰統計，不需要細看。

本版不是新增更多長卡片，而是把既有資訊重排成：

```text
今日結論 -> 明日執行清單 -> 未持倉漏斗 -> 詳情索引
```

## 使用者可見變化

### 1. 總覽摘要新增今日結論

摘要最上方新增單行交易結論：

```text
🧭 今日結論：R3 偏熱，不新增；持倉優先，明日只追 3 檔
🧭 原因：強勢股多過熱，RR不足，不追高
```

若有持倉風控，今日結論不得只寫「不新增」，必須帶出風控語意。

### 2. 新增明日執行清單

把原本的持倉處理優先級、隔日追蹤、待確認候選，合併成一個可掃讀清單。

硬性規則：

- 最多顯示 5 項。
- 停損、減碼、停利、新倉風控、核心風控優先於所有未持倉追蹤。
- 若風控項超過 5 項，不得靜默截斷，需顯示 `另有 N 檔風控見詳情` 或等效文字。
- 合格 `BUY` 必須進入清單前段，不得被等待狀態覆蓋。
- `續抱` 不得單獨出現，必須展開為處理狀態與下一步。
- 明日執行清單中的所有持倉項必須保留目前收益百分比。

持倉項格式必須包含目前收益，例如：

```text
英業達｜+20.04%｜核心風控｜守 59.0
智原｜-0.83%｜新倉風控｜站回 211.5，否則降級
緯創｜+6.91%｜洗盤續抱｜守警戒
```

不得為了壓縮刪除持倉盈虧。新倉風控、減碼後觀察、核心風控、洗盤續抱、續抱觀察都必須帶收益百分比。

未持倉項不需要收益百分比，但必須保留等待 / 不買語意，例如：

```text
光寶科｜等回測｜不追價，回測不破且降溫再評估
建準｜等RR修復｜不追價
```

### 3. 新增未持倉漏斗

未持倉不再只是一串 `不買`，摘要中改成漏斗統計：

```text
未持倉漏斗：
可買 0｜可準備 0｜等回測 1｜等RR修復 2｜等量能 0｜淘汰 4
```

規則：

- `可買` 只允許合格進場標的。
- `可準備` 必須被理解為不可買的準備度，不得等同進場建議。
- `等回測`、`等RR修復`、`等量能` 必須帶 `等`、`不買`、`不追價` 或具體觸發條件。
- 弱勢淘汰不佔用主執行清單，但名稱與主因不可消失。

### 4. 新增詳情索引

摘要底部新增詳情索引，讓使用者知道完整資料在哪裡：

```text
📎 詳情索引：持倉 5｜追蹤 3｜淘汰 4
```

若低優先級未持倉被壓縮，仍需保留名稱與主因，例如：

```text
淘汰 4：旺宏、華邦電、...｜主因：市場弱、遠離觸發
```

12 檔 watchlist 不得因壓縮而在報文中完全消失；每檔至少要能在摘要、詳情或壓縮列表中追溯。

## 新版報文設計

目標摘要範例：

```text
【05/26 收盤｜v19.5】
📊 市場：進攻偏熱｜R3
🧭 今日結論：不新增，持倉優先；明日只追 3 檔
🧭 原因：強勢股多過熱，RR不足，不追高

✅ 明日執行清單
1. 智原｜-0.83%｜新倉風控｜站回 211.5，否則降級
2. 英業達｜+20.04%｜核心風控｜守 59.0，跌破升級風控
3. 緯創｜+6.91%｜洗盤續抱｜守警戒
4. 光寶科｜等回測｜不追價，回測不破且降溫再評估
5. 建準｜等RR修復｜不追價

未持倉漏斗：
可買 0｜可準備 0｜等回測 1｜等RR修復 2｜等量能 0｜淘汰 4

📎 詳情索引：持倉 5｜追蹤 3｜淘汰 4
```

既有持倉詳情與未持倉詳情仍保留，但摘要應成為決策入口，不再把所有欄位同權重 dump 出來。

## 報文 / 流程變更

### 摘要層

v19.5 總覽摘要必須包含：

- `🧭 今日結論`
- `🧭 原因`
- `✅ 明日執行清單`
- `未持倉漏斗`
- `📎 詳情索引`

### 詳情層

詳情層保留完整可追溯資料。

可壓縮：

- 弱勢淘汰。
- 遠離觸發且低優先級的未持倉。

不可壓縮到消失：

- 持倉。
- 停損 / 減碼 / 停利 / 新倉風控 / 核心風控。
- 合格可買。
- 高優先級追蹤。
- 等回測、等RR修復、等量能中被列入明日執行清單者。

### Telegram 推送契約

延續 v19.4.1：

- 預設 `messages[-1]` 必須仍是總覽摘要。
- `reply_markup` 必須仍綁在最後摘要段。
- `include_detail=True` 時，完整詳情 chunk 必須在摘要之前。
- 若摘要發生 split，最後一個 summary chunk 仍必須包含核心結論或可回到核心結論，不得只剩低價值殘文。

## Edge Cases

- 無持倉：明日執行清單以候選為主，並顯示 `無持倉，僅追蹤 N 檔待確認`。
- 無新追蹤：顯示 `僅處理持倉，無新追蹤`。
- 有停損 / 減碼 / 停利：必須排在明日執行清單最前面。
- 風控超過 5 項：顯示前 5 項，並補 `另有 N 檔風控見詳情`。
- 合格 BUY 出現：必須進執行清單前段，且不得顯示為等待狀態。
- 等待標的：必須明確不是可買，例如 `等RR修復｜不追價`。
- 弱勢淘汰很多：可統計化，但保留名稱與主因。
- position_events 缺失：新倉 / 減碼後語意安全回退，不硬判。
- 回測參考度低：不得因相對略優排到高優先級。
- Telegram 摘要過長：優先保證摘要完整可讀，清單最多 5 項，詳情可拆分。
- 單段 string fallback：不得破壞既有 `generate()` join list 行為。

## 影響模組初判

預期主要影響：

- `core/generator.py`
  - summary formatter
  - decision summary view model
  - execution checklist sorting
  - unheld funnel statistics
  - detail index
  - low-priority unheld compression
- `tests/test_generator_report.py`
  - formatter / view model / summary order tests
- `tests/test_notifier.py`
  - `reply_markup` last summary contract regression

直接消費者需被 QA 檢查：

- `generate_report()`
- `formatTelegramMessages()`
- `main.py`
- `services/notifier.send_many()`
- `generate()` list join fallback

不預期影響：

- `services/analysis.py`
- `services/stock_api.py`
- `services/signal_store.py`
- `services/daily_snapshot_store.py`
- `services/position_store.py`
- `scripts/dry_run_replay.py`
- `scripts/backfill_signals.py`
- DB schema
- 股票池

## 不可變更範圍

本任務不可變更：

- 不改策略 action。
- 不改 `decision`。
- 不改 `is_tradeable`。
- 不改 `is_best_candidate`。
- 不改 RR / 過熱 / 漲停不追 / 加碼 / 停利 / 停損硬門檻。
- 不改 `services/analysis.py` 的交易判斷。
- 不改 DB schema。
- 不新增跨日 tracking table。
- 不正式寫庫。
- 不改 replay/backfill。
- 不擴大股票池。
- 不把回測變成 BUY 來源。
- 不讓 `可準備` 變成買入建議。
- 不刪除持倉目前收益百分比。
- 不讓任何 watchlist 標的從報文可追溯範圍中消失。

## 驗收標準

v19.5 需滿足：

1. `version_level` 為 `minor`。
2. `qa_level` 預設 `L3`；若明確降級，實測不得低於 `L2 顯示不變性 + Telegram contract`。
3. 總覽摘要新增 `🧭 今日結論`。
4. 總覽摘要新增 `✅ 明日執行清單`。
5. 總覽摘要新增 `未持倉漏斗`。
6. 總覽摘要新增 `📎 詳情索引`。
7. 明日執行清單最多顯示 5 項。
8. 停損 / 減碼 / 停利 / 新倉風控 / 核心風控優先於未持倉追蹤。
9. 風控超過 5 項時，必須顯示 `另有 N 檔風控見詳情` 或等效提示。
10. 明日執行清單中的所有持倉項必須保留目前收益百分比，例如 `英業達｜+20.04%｜核心風控｜守 59.0`。
11. 新倉風控、減碼後觀察、核心風控、洗盤續抱、續抱觀察都必須帶收益百分比。
12. 未持倉項不需要收益百分比，但必須保留等待 / 不買 / 不追價語意。
13. 合格 `BUY` 必須進入執行清單前段，不得被等待狀態覆蓋。
14. `續抱` 不得單獨出現在摘要，必須有處理狀態或下一步。
15. `可準備`、`等回測`、`等RR修復`、`等量能` 不得被誤解為可買。
16. 弱勢淘汰可壓縮，但股票名稱與主因不可消失。
17. 12 檔 watchlist 每檔都能在摘要、詳情或壓縮列表中追溯。
18. 回測只能影響同類追蹤排序，不得直接產生 BUY。
19. 不改 strategy action / decision / is_tradeable / is_best_candidate。
20. 不改 RR / 過熱 / 漲停不追 / 加碼 / 停利 / 停損硬門檻。
21. 不改 DB schema、股票池、replay/backfill。
22. 預設 messages list 中，總覽摘要仍為 `messages[-1]`。
23. `reply_markup` 仍綁在最後摘要段。
24. `include_detail=True` 時，完整詳情 chunk 仍在摘要之前。
25. `generate_report()` 回傳契約可被 `main.py -> send_many()` 正常消費。
26. 單段 string 行為不得回退。
27. 摘要不得超過 Telegram 單段截斷風險；若 split，必須定義最後 summary chunk 規則。
28. Tech 需在 `CHANGELOG.md` 明確列出是否只改顯示 / 排序 / summary view model，並列出直接消費者是否同步。

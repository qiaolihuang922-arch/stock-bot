# TASK: intraday_report_state_readability_v21_1_20260624

## Task Status

- task_id: `intraday_report_state_readability_v21_1_20260624`
- task_type: `normal_patch`
- status: `implemented`
- version_contract: report header remains `v21.1`
- QA level: L2 focused formatter + official dry-run

## Owner Problem

The 06/24 intraday report still had three mobile-reading conflicts:

- Holding cards were in `盤中` but said `明日處理`.
- 光寶科 moved from `可買` to ordinary `等低位修復` when price slipped just below the 5-day MA, even though support and volume were still valid and the missing gap was tiny.
- 旺宏 / similar failed-breakout names close to the reclaim zone were displayed as terminal `淘汰`, even though the useful instruction was to wait for a clearly named reclaim zone.

## User-Visible Result

- Intraday holding cards now show `盤中處理`.
- Low-repair cards that only miss the 5-day MA by a small tolerance show `貼近可買｜低位修復接近成立`.
- Failed-breakout cards within the reclaim-zone threshold show `等站回｜突破失敗`, with the exact zone and gap.
- `等站回` cards are compact: no trade-state line and no machine-style data line.

## Non-Goals

- No DB schema change.
- No production DB write, backfill, prune, or dedupe.
- No live Telegram delivery.
- No buy threshold relaxation: `貼近可買` is not `可買`.
- No version bump beyond visible `v21.1`.

## Impacted Modules And Consumers

- `core/generator.py`: low-repair near-ready classification, failed-breakout reclaim routing, funnel buckets / summary counts.
- `core/trade_state_machine.py`: state mapping for `等站回`.
- `presentation/report.py`: phase-aware holding handling label and compact card rendering.
- `tests/test_generator_report.py`: regression specimens for the Owner-visible issues.
- Direct consumer: official Telegram message list generated by `generate_report`.

## Output Contract

- `盤中` holding cards use `盤中處理`; `盤前` uses `盤前處理`; after-hours / close remains `明日處理`.
- Low-repair near-ready remains non-actionable until the 5-day MA is reclaimed, but displays:
  - `貼近可買｜低位修復接近成立`
  - `5日均 ... 貼近（差 ... / ...%）`
  - `盤中觸發：站回5日均 ... 可小倉；未站回先觀察`
- Failed breakout with a known reclaim anchor and distance within 5% displays `等站回`, not `淘汰`.
- Failed breakout without a known reclaim anchor remains blocked / rejected; no fake zone is invented.

## Acceptance Conditions

- Focused tests for low-repair near-ready, failed-breakout reclaim, and phase-aware holding labels pass.
- Broader report subset for low-repair / failed-breakout / holding next-step contracts passes.
- Official dry-run produces:
  - `貼近可買`
  - `等站回`
  - no `明日處理：` inside the 06/24 intraday holding message.

## Failure Specimen And Validation Route

- Failure layer: official Telegram report rendering.
- Owner specimen: 06/24 intraday report where 光寶科 oscillated between buy / wait and 旺宏 remained淘汰 despite being a reclaim-zone watch.
- Validation route: formatter regression + funnel state regression + official dry-run message-list artifact.

## Forbidden Actions

- Do not write production DB.
- Do not send live Telegram.
- Do not turn near-ready into buyable.
- Do not hide the breakout distance line.

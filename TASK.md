# TASK: v19.5.1 摘要語意一致性修復

## 時間性

- 任務日期：2026-05-26
- 來源任務：`DISPATCH.md` Current Task / Task Brief
- 目標版本：`v19.5.1`
- 任務性質：摘要文案與跨區塊語意一致性修復
- version_level：`patch`
- qa_level：`L1`
- 狀態：PM 正式需求，等待 Architect 交 Tech
- 邊界：只修 formatter / summary view model 的顯示語意與必要局部測試；不改策略、不改買賣判斷、不改 DB、不改 replay/backfill。

## 需求目標

v19.5 已完成「今日結論、明日執行清單、未持倉漏斗、詳情索引」的摘要架構，但實際報文出現跨區塊語意衝突：

```text
今日結論：明日只追 6 檔
明日執行清單：5 項全是持倉
補充文字：另有 6 檔追蹤見詳情
未持倉漏斗：6 檔只存在於漏斗 / 詳情
```

使用者會誤解：

- `追 6 檔` 是不是明天要執行的 6 件事。
- 明日執行清單是否漏掉 6 檔未持倉。
- `另有 6 檔追蹤` 是否和執行清單同等優先。
- 漏斗中的未持倉是否可買或需要行動。

v19.5.1 目標是修正這種語意不一致，讓摘要清楚區分：

```text
明日執行項 = 需要主動處理
僅追蹤候選 = 不買、不追價、只等待條件
漏斗統計 = 背景分類，不等於執行清單
```

## 使用者可見變化

### 1. 今日結論不得混用「執行」與「追蹤」

若明日執行清單 5 項全是持倉，今日結論不得寫：

```text
明日只追 6 檔
```

應改成能同時描述執行與追蹤的文字：

```text
🧭 今日結論：持倉優先處理；未持倉 6 檔僅追蹤，不新增
```

或：

```text
🧭 今日結論：明日執行 5 項持倉；未持倉只等觸發
```

規則：

- `執行` 只用於明日執行清單內的項目。
- `追蹤` 只用於未進入執行清單、尚不可買的候選。
- `明日只追 N 檔` 只能在清單內確實有 N 檔追蹤項，且文案不會被理解成買入時使用。
- 若未持倉只是漏斗候選，今日結論必須寫 `僅追蹤`、`等觸發`、`不新增` 或等效語意。

### 2. 明日執行清單需明確標示清單範圍

明日執行清單若全部是持倉，標題或補充行需清楚說明：

```text
✅ 明日執行清單（持倉優先）
1. 英業達｜+20.04%｜核心風控｜守 59.0
2. 緯創｜+6.91%｜洗盤續抱｜守警戒
...
另有 6 檔未持倉僅追蹤，見漏斗 / 詳情
```

不可使用會讓使用者以為未持倉也是同級執行項的文案，例如：

```text
另有 6 檔追蹤見詳情
```

應改成：

```text
另有 6 檔未持倉僅追蹤，尚未進入執行清單
```

或：

```text
未持倉 6 檔只等觸發，不列入明日執行
```

### 3. 未持倉漏斗需明確不是執行清單

漏斗標題或內容需避免被理解為買入清單。

建議格式：

```text
未持倉漏斗（非執行）：
可買 0｜僅追蹤 6｜等回測 2｜等RR修復 2｜淘汰 2
```

或：

```text
未持倉漏斗：
可買 0｜不可買追蹤 6｜淘汰 2
```

規則：

- 若 `可買` 為 0，今日結論與漏斗都不得暗示新增。
- `僅追蹤`、`不可買追蹤`、`等觸發` 可用來取代模糊的 `追 N 檔`。
- `可準備` 若保留，必須搭配不可買語意，例如 `可準備 2（不可買）`。

### 4. 詳情索引需與摘要數字一致

詳情索引應能回答：

- 持倉幾檔。
- 明日執行幾項。
- 未持倉僅追蹤幾檔。
- 淘汰幾檔。

建議格式：

```text
📎 詳情索引：持倉 5｜執行 5｜未持倉追蹤 6｜淘汰 1
```

若執行清單全是持倉，索引不得讓使用者誤以為還有 6 個同級執行項。

## 報文 / 流程設計

### 修正前問題範例

```text
🧭 今日結論：不新增，持倉優先；明日只追 6 檔

✅ 明日執行清單
1. 英業達｜+20.04%｜核心風控｜守 59.0
2. 緯創｜+6.91%｜洗盤續抱｜守警戒
3. 技嘉｜+0.07%｜洗盤續抱｜守警戒
4. 南亞科｜+2.35%｜續抱觀察｜降級觀察
5. 智原｜-0.83%｜新倉風控｜站回 211.5
另有 6 檔追蹤見詳情
```

問題：

- 今日結論說 `追 6 檔`，但清單裡沒有 6 檔未持倉。
- `另有 6 檔追蹤` 沒說明是不可買候選。
- 使用者會把 `追蹤` 理解成明日要處理的執行項。

### 修正後目標範例

```text
🧭 今日結論：持倉優先處理；未持倉 6 檔僅追蹤，不新增

✅ 明日執行清單（持倉優先）
1. 英業達｜+20.04%｜核心風控｜守 59.0
2. 緯創｜+6.91%｜洗盤續抱｜守警戒
3. 技嘉｜+0.07%｜洗盤續抱｜守警戒
4. 南亞科｜+2.35%｜續抱觀察｜降級觀察
5. 智原｜-0.83%｜新倉風控｜站回 211.5
未持倉 6 檔只等觸發，不列入明日執行

未持倉漏斗（非執行）：
可買 0｜不可買追蹤 6｜淘汰 1

📎 詳情索引：持倉 5｜執行 5｜未持倉追蹤 6｜淘汰 1
```

### 保留 v19.5 既有要求

本輪不得回退 v19.5 已完成能力：

- `🧭 今日結論`
- `✅ 明日執行清單`
- `未持倉漏斗`
- `📎 詳情索引`
- 持倉執行清單保留目前收益百分比
- 總覽摘要最後送出
- `reply_markup` 綁定最後摘要段

## Edge Cases

- 執行清單全是持倉：今日結論需寫 `持倉優先`，未持倉只能寫 `僅追蹤 / 等觸發 / 不新增`。
- 執行清單同時有持倉與未持倉：今日結論需分開描述 `執行 N 項，其中持倉 X、候選 Y`。
- 未持倉可買為 0：不得出現會被理解成買入的 `追 N 檔`。
- 未持倉存在合格 BUY：可進入執行清單，且今日結論可以寫 `可買 N`，但仍需和漏斗數字一致。
- 執行清單因 5 項上限截斷：overflow 文案需區分 `另有 N 項風控` 與 `另有 N 檔僅追蹤候選`。
- 無未持倉追蹤：不要顯示 `追 0 檔` 造成噪音，可寫 `未持倉無追蹤`。
- 無持倉：明日執行清單可改以候選為主，但必須明確區分 `可買` 與 `等待`。
- 漏斗、詳情索引、執行清單的數量必須互相對得上。

## 影響模組初判

預期主要影響：

- `core/generator.py`
  - `今日結論` 文案
  - `明日執行清單` overflow 文案
  - `未持倉漏斗` 標題 / 類別文案
  - `詳情索引` 數量與命名
  - summary view model 的顯示欄位命名
- `tests/test_generator_report.py`
  - 摘要語意一致性測試
  - formatter snapshot-like 局部測試

QA 需檢查直接消費者但本輪不預期修改：

- `formatTelegramMessages()`
- `generate_report()`
- `main.py -> send_many()`
- `services/notifier.send_many()`

不預期影響：

- `services/analysis.py`
- `services/stock_api.py`
- `services/signal_store.py`
- `services/daily_snapshot_store.py`
- `services/position_store.py`
- `scripts/dry_run_replay.py`
- `scripts/backfill_signals.py`
- DB schema
- 股票池

## 不可變更範圍

本任務不可變更：

- 不改策略 action。
- 不改買入 / 賣出 / 加碼 / 減碼 / 停利 / 停損判斷。
- 不改 RR / 過熱 / 漲停不追 / 加碼 / 停利 / 停損硬門檻。
- 不改 `services/analysis.py`。
- 不改 DB schema。
- 不新增跨日 tracking table。
- 不正式寫庫。
- 不改 replay/backfill。
- 不擴大股票池。
- 不改 v19.5 的 Telegram summary-last contract。
- 不移除持倉盈虧百分比。
- 不把 `僅追蹤候選` 改成買入建議。

## 驗收標準

v19.5.1 需滿足：

1. `version_level` 為 `patch`。
2. `qa_level` 為 `L1`。
3. 今日結論不得在未持倉不可買時寫成容易誤解的 `明日只追 N 檔`。
4. 今日結論需清楚區分 `明日執行項` 與 `僅追蹤候選`。
5. 明日執行清單若全是持倉，標題或補充行需明確寫出 `持倉優先`。
6. 明日執行清單 overflow 不得只寫 `另有 N 檔追蹤見詳情`，需寫明 `未持倉僅追蹤 / 等觸發 / 不列入明日執行`。
7. 未持倉漏斗需標明非執行或不可買語意，例如 `未持倉漏斗（非執行）`、`不可買追蹤`、`僅追蹤`。
8. `可買 0` 時，摘要不得暗示新增或買入。
9. `可準備` 若出現，需帶 `不可買` 或等效語意。
10. 詳情索引需區分 `執行`、`未持倉追蹤`、`淘汰`，且數字與摘要 / 漏斗一致。
11. 持倉執行清單仍必須保留目前收益百分比。
12. 總覽摘要仍為 `messages[-1]`。
13. `reply_markup` 仍綁在最後摘要段。
14. 不改 strategy action / decision / is_tradeable / is_best_candidate。
15. 不改 DB schema、股票池、replay/backfill。
16. QA 除 L1 formatter 檢查外，需按 `AGENTS.md` 做跨區塊語意一致性與使用者誤讀風險檢查。

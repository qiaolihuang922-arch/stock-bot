# TASK: report_actionability_readability_v21_1_20260624

## Task Status

- task_id: `report_actionability_readability_v21_1_20260624`
- task_type: `normal_patch`
- status: `in_progress`
- version_contract: report header remains `v21.1`
- QA level: L2 official report readability + formatter regressions

## Owner Problem

Owner's 06/24 intraday report still fails the mobile reader contract:

- `貼近可買` reads like a buy signal even when the card says the stock has not reclaimed the 5-day MA.
- Low-repair cards repeat already-satisfied conditions in the trigger line instead of showing only the missing condition.
- `量能 1x OK` is too coarse; it does not tell whether volume is barely acceptable or truly effective.
- Failed-breakout cards near the reclaim zone can flip between `等站回` and `淘汰` because the distance cutoff is too hard.
- Some cards leak engineering history such as `前次 eliminated｜權重 +1`.
- Summary and backtest lines can add non-decision noise, making the useful action harder to see.

## User-Visible Result

- A near low-repair setup must say `貼近條件｜等站回5日均`, not `貼近可買`.
- Low-repair trigger line must list only missing conditions. If only 5-day MA is missing, only show reclaiming the 5-day MA.
- Volume labels must be human-readable: `不足`, `剛好`, `有效`, `攻擊量`.
- Failed breakout within the near reclaim band should remain watchable as `等站回`, not terminal `淘汰`.
- Engineering history terms must be hidden or translated to reader-facing wording.
- Summary should stay decision-first and avoid standalone backtest noise unless there is an actual actionable candidate.

## Non-Goals

- No production DB write, backfill, prune, dedupe, or schema change.
- No live Telegram delivery.
- No change to v21.1 header.
- No relaxation of actual buy eligibility: only `可買` is actionable.

## Impacted Modules And Consumers

- `core/generator.py`: failed-breakout reclaim threshold / bucket classification.
- `presentation/report.py`: Telegram card labels, low-repair lines, history filtering, summary/backtest noise.
- `tests/test_generator_report.py`: Owner-visible regression coverage.
- Direct consumer: official Telegram message list generated by `generate_report` / `formatTelegramMessages`.

## Output Contract

- Low-repair near-ready card:
  - title: `👀 貼近條件｜等站回5日均`
  - line: `低位修復：支撐 ... OK｜5日均 ... 貼近...｜量能 ...`
  - trigger: `盤中觸發：站回5日均 ... 可小倉；未站回先觀察`
  - must not contain `貼近可買`.
- Low-repair wait card:
  - trigger lists only missing gates, not already satisfied support / volume.
  - volume uses qualitative labels.
- Failed-breakout card:
  - within 7% of a real reclaim zone can display `等站回`.
  - must not invent a zone.
  - must not show `攻擊量` as positive evidence on a failed breakout.
- Summary:
  - no standalone backtest line for non-actionable `可準備` / `準備觀察`.
  - no engineering history terms such as `eliminated`.

## Acceptance Conditions

- Focused formatter/generator tests for low-repair near-ready, missing-only triggers, failed-breakout reclaim, and history filtering pass.
- Official dry-run shows:
  - no `貼近可買`;
  - if low-repair near-ready exists, it uses `貼近條件`;
  - no `明日處理：` inside intraday holding message;
  - no engineering `前次 eliminated` text.

## Failure Specimen And Validation Route

- Failure layer: official Telegram report rendering.
- Owner specimens: 06/24 intraday and 06/23 after-hours reports pasted in chat.
- Validation route: formatter regression + funnel state regression + official dry-run message-list artifact.

## Forbidden Actions

- Do not write production DB.
- Do not send live Telegram.
- Do not treat `貼近條件` or `準備觀察` as actionable buy.
- Do not remove the `距突破` line.

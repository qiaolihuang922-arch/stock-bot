# CHANGELOG

## 2026-05-26 - v19.3.4 報文解釋力修正

### 修改內容
- 依 `TASK.md` 實作 v19.3.4 formatter 顯示層修正，版本顯示更新為 `v19.3.4`。
- 回測行補上可讀判讀：
  - `樣本 < 10` 顯示 `參考度低`。
  - `10 <= 樣本 < 30` 顯示 `參考度中`。
  - `樣本 >= 30` 顯示 `參考度高`。
  - 依 3 日相對報酬補上 `略優 / 無明顯優勢 / 偏弱 / 判讀不足`。
- R3 且今日重點為暫不新增時，摘要新增 `🧭 原因`，說明新倉不開的主因，例如過熱、RR 不足、禁止追高。
- 今日新倉且浮虧時，不再顯示普通續抱：
  - 一般新倉浮虧顯示 `新倉風控觀察`。
  - 新倉浮虧且符合洗盤警戒條件時顯示 `洗盤警戒`。
  - 詳情補上 `決策 / 條件 / 下一步`。
- 持倉詳情卡新增 `下一步`。
- 停利 / 減碼 / 停損詳情卡新增 `原因` 與 `下一步`，避免只顯示交易動作但缺少處理邏輯。
- 保留 v19.3.x 大版型：
  - 市場摘要
  - 持倉摘要
  - 未持倉分組
  - 持倉標的
  - 未持倉標的

### 修改檔案
- `core/generator.py`
- `tests/test_generator_report.py`
- `CHANGELOG.md`

### 未影響模組
- `services/analysis.py`
- 策略門檻
- RR 計算與門檻
- scoring
- snapshot
- DB schema
- DB 寫入邏輯
- replay / backfill
- 股票池
- Telegram 大版型
- Supabase Edge Function
- TWSE/Yahoo provider 底層請求邏輯
- v19.4 策略方向

### 風險點
- 本次只修 formatter 解釋力，不會改變實際買賣判斷。
- 新倉浮虧依賴 `position_events.bought_shares`；若今日交易事件未入庫，報文無法判定「今日新倉」。
- R3 原因行是摘要解釋，不是新的策略阻斷條件。
- 回測 `參考度` 與 `略優 / 偏弱` 只來自既有 backtest context，不新增或改寫回測資料。
- 本次未跑 full regression、live Telegram、DB、replay/backfill。

### 建議 QA 驗證範圍
- v19.3.4 盤中 / 盤後報文版本顯示。
- R3 且無新倉時，摘要是否出現 `🧭 原因`，且不造成第一則摘要過長。
- 回測行是否顯示：
  - 樣本數
  - 參考度
  - 3 日勝率
  - 相對報酬
  - 判讀結果
- 今日新倉浮虧：
  - 顯示 `新倉風控觀察` 或 `洗盤警戒`。
  - 詳情包含 `決策 / 條件 / 下一步`。
- 停利 / 減碼 / 停損：
  - 詳情包含 `原因`。
  - 詳情包含 `下一步`。
  - 不改變原本交易 action。
- 價格行右括號仍完整。
- 報文大版型不得回退。
- 已執行最低必要驗證：
  - `.venv/bin/python -m pytest tests/test_generator_report.py`
  - 結果：`29 passed`

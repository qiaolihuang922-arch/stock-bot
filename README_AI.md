# AI Maintenance Entry

本文件保留作旧入口，避免 AI 读取过期规则。

当前维护规则以 `AI_CONTEXT.md` 为准：

- `18.x`：稳定策略层、显示层、行情准确性。
- `19.x`：使用 `services/signal_store.py` 记录每日收盘信号与结果回填。
- 默认不处理 `services/ai.py` 与 `services/learning.py`。
- `config.py` 含密钥，不要展开或修改。

继续维护前先读 `AI_CONTEXT.md`。
